name: CI/CD Pipelane
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Print to console
        run: echo Hello Github, aaaa


